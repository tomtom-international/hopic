version:
  bump: false

phases:
  build:
    example:
      - archive:
          artifacts:
            produce_output.txt
          upload-on-failure: true
      - touch produce_output.txt
      - "false"